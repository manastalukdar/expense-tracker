{
  "name": "@expense-tracker/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared types, utilities, and constants for expense tracker",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts,.tsx",
    "lint:fix": "eslint src --ext .ts,.tsx --fix",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "eslint": "^9.31.0",
    "jest": "^30.0.4",
    "typescript": "^5.8.3"
  },
  "files": [
    "dist"
  ]
}